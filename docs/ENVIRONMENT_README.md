# 環境說明

## 當前環境限制

本專案在**容器/開發環境**中由於代理限制無法訪問 HITCON 網站。

### 診斷結果

運行 `python diagnose_network.py` 顯示：

```
❌ HITCON 網站無法訪問
可能原因: 代理封鎖了該網站
```

這是**環境特定問題**，不影響本地使用。

## 本地環境運行

### 在您的電腦上運行

```bash
# 1. 克隆專案
git clone https://github.com/dwvwdv/HITCON-Vuls-Crawler.git
cd HITCON-Vuls-Crawler

# 2. 安裝依賴
pip install -r requirements.txt

# 3. 運行診斷（檢查網絡）
python diagnose_network.py

# 4. 運行 TUI 程式
python tui_app.py
```

### 預期行為

**本地環境（正常）：**
```
✅ HITCON 網站可訪問
✅ 爬蟲應該能正常工作
```
- 狀態欄：`Page: 1 | Vulnerabilities: 20`（無"演示模式"標籤）
- 顯示真實漏洞數據

**受限環境（當前）：**
```
❌ HITCON 網站無法訪問
當前狀態: 程式會自動使用演示模式
```
- 狀態欄：`Page: 1 | Vulnerabilities: 20 | 演示模式`
- 顯示測試數據

## 診斷工具

### 運行網絡診斷

```bash
python diagnose_network.py
```

### 診斷內容

1. **基本網絡連接** - 測試是否能訪問網絡
2. **代理設置檢查** - 檢測環境變數中的代理配置
3. **HITCON 訪問測試** - 嘗試多種方式訪問網站
4. **HTML 解析測試** - 驗證數據能否正確解析

## 環境對比

| 環境類型 | 代理 | 訪問狀態 | 數據來源 |
|---------|------|---------|---------|
| 本地開發環境 | 無 | ✅ 正常 | 真實數據 |
| 公司/學校網絡 | 可能有 | ⚠️ 視情況 | 可能需要配置 |
| 容器/CI環境 | 通常有 | ❌ 可能受限 | 演示數據 |
| VPN 環境 | 依VPN | ✅ 通常正常 | 真實數據 |

## 為什麼使用演示模式？

演示模式是一個**智能後備方案**：

✅ **無縫體驗** - 程式能正常運行，所有功能可測試
✅ **開發友好** - 不依賴網絡也能開發和測試
✅ **清晰標示** - 狀態欄明確顯示"演示模式"
✅ **自動切換** - 檢測到網絡問題自動啟用

## 如何獲取真實數據

### 方法 1：本地環境運行（推薦）

直接在您的電腦上運行，通常沒有代理限制。

### 方法 2：配置代理白名單

如果您的網絡管理員可以配置：

```bash
# 添加到 no_proxy
export no_proxy="$no_proxy,zeroday.hitcon.org"
```

### 方法 3：使用 VPN

連接到台灣或其他可訪問地區的 VPN。

### 方法 4：查看原始代碼邏輯

`crawler.py` 中的自動回退邏輯：

```python
def get_vulnerabilities(self, page_num: int) -> List[Vulnerability]:
    # 嘗試獲取真實數據
    html = self.fetch_page(page_num)

    # 失敗時自動切換到演示模式
    if html is None:
        self.use_demo_data = True
        return self._generate_demo_data(page_num)

    # 成功返回真實數據
    return self.parse_vulnerabilities(html)
```

## 驗證真實數據

### 檢查狀態欄

**真實數據：**
```
Page: 1 | Vulnerabilities: 20 | Press ? for help
```

**演示數據：**
```
Page: 1 | Vulnerabilities: 20 | 演示模式 | Access Denied (403)
```

### 檢查漏洞標題

**真實數據：**
```
CVE-2024-XXXXX: [真實漏洞標題]
```

**演示數據：**
```
[示例] Vulnerability #1 - 這是測試數據 (網站無法訪問時的演示)
```

## 技術細節

### 代理檢測

程式會檢測以下環境變數：
- `HTTP_PROXY` / `http_proxy`
- `HTTPS_PROXY` / `https_proxy`
- `NO_PROXY` / `no_proxy`

### 重試邏輯

```python
# 1. 使用代理嘗試
response = self.scraper.get(url, timeout=15)

# 2. 如果代理返回 403，嘗試繞過代理
session.proxies = {'http': None, 'https': None}
session.trust_env = False
response = session.get(url, timeout=15)

# 3. 都失敗則使用演示數據
return self._generate_demo_data(page_num)
```

## 常見問題

### Q: 為什麼我看到演示數據？

**A:** 當前環境無法訪問 HITCON 網站。運行 `python diagnose_network.py` 查看詳細原因。

### Q: 演示數據是否影響功能測試？

**A:** 不影響。所有鍵位和功能都能正常測試，只是數據不是真實漏洞。

### Q: 如何確認代碼在本地能正常工作？

**A:**
1. 在本地克隆專案
2. 運行 `python diagnose_network.py`
3. 如果顯示"HITCON 網站可訪問"，則代碼能正常工作

### Q: 可以禁用演示模式嗎？

**A:** 可以，但不推薦。如果想強制禁用：

```python
# crawler.py - 修改 get_vulnerabilities 方法
def get_vulnerabilities(self, page_num: int) -> List[Vulnerability]:
    html = self.fetch_page(page_num)
    if html is None:
        return []  # 返回空列表而不是演示數據
    return self.parse_vulnerabilities(html)
```

## 總結

📊 **當前狀態：**
- 容器環境：使用演示模式（代理限制）
- 本地環境：應能獲取真實數據

🎯 **建議：**
- 在本地運行以獲取真實數據
- 使用演示模式測試功能
- 運行診斷工具確認環境狀態

✅ **代碼質量：**
- 代碼本身沒有問題
- 智能後備機制完善
- 本地環境應能正常工作
